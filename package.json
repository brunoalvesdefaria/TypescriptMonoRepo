{
  "private": true,
  "scripts": {
    "postinstall": "(cd shared/lib && yarn); (cd ui/web && yarn)"
  },
  "devDependencies": {
    "@parcel/packager-ts": "^2.0.1",
    "@parcel/transformer-typescript-types": "^2.0.1",
    "typescript": ">=3.0.0"
  }
}
